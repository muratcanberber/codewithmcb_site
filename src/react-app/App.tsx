// src/App.tsx

import "./App.css";

function App() {
  const [count, setCount] = useState(0);
  const [name, setName] = useState("unknown");

  return (
    <>
<h1>Vite + React + Hono + Cloudflare</h1>

  );
}

export default App;
